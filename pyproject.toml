[project]
name = "netlink"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "cryptography>=48.0.0",
    "macaddress>=2.0.2",
    "rich>=14.3.3",
    "scapy>=2.7.0",
]

[project.scripts]
netlink = "netlink.cli:main"
